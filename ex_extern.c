#include <stdio.h>
#include "helper.h" // 사용할 helper 함수의 선언을 포함한 헤더 파일을 포함합니다.

// helper 함수를 사용하여 메인 함수를 정의합니다.
int main() {
    int result = multiplyByTwo(5); // helper 함수 호출
    printf("Result: %d\n", result);
    return 0;
}
